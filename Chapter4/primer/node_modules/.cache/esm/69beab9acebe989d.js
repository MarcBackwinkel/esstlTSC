let calculateTax;_ceb‍.x([["printDetails",()=>printDetails],["applyDiscount",()=>applyDiscount]]);_ceb‍.w("./tax",[["calculateTax",["calculateTax"],function(v){calculateTax=v}]]);

       function printDetails(product){
    let taxedPrice = calculateTax(product.price);
    console.log(`Name: ${product.name}, Taxed Price: ${taxedPrice}`);
}

       function applyDiscount(product, discount = 5){
    product.price -= 5; 
}
